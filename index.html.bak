<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/static/css/index.css">
    <title>MyAlbum</title>
</head>
<body>

<div id="wrapper"> 
     <div id="content">
        <div class="center">
            <h2>My Album</h2> 
        </div>
        <div>
        {{range $index, $elem := .thumb }}
            <a href="photo/{{$elem.PathBase64}}">
            <img src="data:image/jpeg;base64,{{$elem.Thumbnail}}" />
            </a>
        {{end}}
        </div>
        <div>
            {{range $index, $pageNumber := .pagers.Before}}
                <li>
                    <a href="?page={{$pageNumber}}">{{$pageNumber}}</a>
                </li>
            {{end}}
            <li><span style="color:red">{{ .pagers.Current }}</span></li>
            {{range $index, $pageNumber := .pagers.After}}
            <li><a href="?page={{$pageNumber}}">{{$pageNumber}}</a></li>
            {{end}}

            {{if .currentyear}} <!-- http://192.168.0.199:8080/?page=63 -->
                    {{.currentyear}}
                    {{else}} <!--http://192.168.0.199:8080/year?year=2018&page=1 -->

            {{end}}
            </p>
        </div>
    </div>
    <div id="sidebar">
        <h3>Photo List</h3>
        {{range $index, $elem := .years }}
            <a href="/year?year={{$elem.Year}}&page=1">
            <p>{{$elem.Year}} - ({{$elem.Number}})</p> 
            </a>
        {{end}}
    </div>
    <div id="cleared"></div>
</div>
</body>
</html>
