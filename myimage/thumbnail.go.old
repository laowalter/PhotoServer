package myimage
/*

import "fmt"


	//	client, ctx := mymongo.ConnectDB("mongodb://localhost:27017") //Just connect monogodb
	//	db := &mymongo.DBconn{Client: client, Ctx: ctx, DBName: "persons"}

	path := "/home/walter/go/src/github.com/photoServer/testData"
	files := ListPicVideoFilesTree(path)
	fmt.Println(files)
	/*
		total := len(files)
		for index, file := range files {
			fmt.Printf("%v/%v\n", index, total)
			img, err := imaging.Open(file)
			if err != nil {
				panic(err)
			}
			fmt.Println(file)
			_ = imaging.Thumbnail(img, 200, 200, imaging.CatmullRom)
		}
	*/
}
