<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/static/css/index.css?v=1.1">
    <script src="/static/css/jquery-3.4.1.slim.min.js"></script> 
    <title>MyAlbum</title>
    <script>
        $(function(){
            $(".imgfullscreen").on("click", function(){
                parent.history.back();
            })
        })
    </script>
</head>
<body>
    <div id="container">
        <img class="imgfullscreen" src="data:image/jpeg;base64,{{.thumb}}" id="responsive-image"/>
    </div>
    <div id="navbar">
        <p>文件路径: {{.photo.Path}}</p>
        <p>{{if .photo.CreateDate}}拍摄时间: {{.photo.CreateDate}}{{end}}</p>
        <p>{{if .photo.FullImageSize}}图像尺寸: {{.photo.FullImageSize}}{{end}}</p>
        <p>{{if .photo.Make}}相机厂商: {{.photo.Make}}{{end}}</p>
        <p>{{if .photo.Model}}相机型号: {{.photo.Model}}{{end}}</p>
        <p>{{if .photo.ShutterSpeed}}快门速度: {{.photo.ShutterSpeed}}{{end}}</p>
        <p>{{if .photo.ISO}}ISO: {{.photo.ISO}}{{end}}</p>
        <p>{{if .photo.Aperture}} 光圈: {{.photo.Aperture}}{{end}}</p>
        <p>{{if .photo.ExposureCompensation}}曝光补偿: {{.photo.ExposureCompensation}}{{end}}</p>
        <p>{{if .photo.FocalLength}}焦距: {{.photo.FocalLength}}{{end}}</p>
        <p>{{if .photo.LensID}}镜头型号: {{.photo.LensID}}{{end}}</p>
        <p>{{if .photo.Longitude}}GPS: 经度:{{.photo.Longitude}}  纬度:{{.photo.Latitude}}{{end}}</p>
    </div>
</body>
</html>
